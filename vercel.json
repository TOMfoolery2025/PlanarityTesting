{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install -r requirements.txt && rm -rf /tmp/matplotlib-*" 
      }
    }
  ],
  "routes": [
    { "src": "/planarity", "dest": "api/index.py" },
    { "src": "/", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}


